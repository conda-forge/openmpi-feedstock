{% set version = "5.0.1" %}
{% set major = version.rpartition('.')[0] %}
{% set cuda_major = (cuda_compiler_version|default("11.8")).rpartition('.')[0] %}
{% set build = 1 %}

# give conda package a higher build number
{% if mpi_type == 'conda' %}
{% set build = build + 100 %}
{% endif %}

package:
  # must not match any outputs for requirements to be handled correctly
  name: openmpi-mpi
  version: {{ version }}

source:
  fn: openmpi-{{ version }}.tar.bz2
  url: https://www.open-mpi.org/software/ompi/v{{ major }}/downloads/openmpi-{{ version }}.tar.bz2
  sha256: e357043e65fd1b956a47d0dae6156a90cf0e378df759364936c1781f1a25ef80

build:
  number: {{ build }}
  skip: true  # [win]
  skip: true  # [enable_cuda and cuda_compiler_version != "11.8"]

outputs:
  {% if mpi_type == 'conda' %}
  - name: openmpi
    script: build-mpi.sh
    build:
      run_exports:
        - {{ pin_subpackage('openmpi', min_pin='x.x.x', max_pin='x') }}
      ignore_run_exports:
        - ucx  # [enable_ucx]
      ignore_run_exports_from:
         - {{ compiler('cuda') }}  # [enable_cuda]
    requirements:
      build:
        - {{ compiler('c') }}
        - {{ compiler('cxx') }}
        - {{ compiler('cuda') }}     # [enable_cuda]
        - {{ compiler('fortran') }}
        #- autoconf  # [unix]
        #- automake  # [unix]
        - libtool   # [unix]
        - make  # [unix]
      host:
        #- openpmix
        #- prrte
        - libhwloc
        - libevent
        - libnl  # [linux]
        - zlib
        - ucx  # [enable_ucx]
      run:
        - mpi 1.0 openmpi
        #- openpmix
        #- prrte
      run_constrained:
        - {{ pin_compatible("ucx", max_pin="x.x") }}  # [enable_ucx]
        # Open MPI only uses CUDA Driver APIs, set the minimal driver version
        - __cuda  >= {{ cuda_major ~ ".0" }}  # [enable_cuda]
        # Ensure a consistent CUDA environment
        - cuda-version  >= {{ cuda_major ~ ".0" }}  # [enable_cuda]
        - cudatoolkit   >= {{ cuda_major ~ ".0" }}  # [enable_cuda]
    test:
      script: run_test.sh
      files:
        - tests/helloworld.sh

  - name: openmpi-mpicc
    build:
      script:
        - echo 'ok'  # [not osx]
        # make CONDA_BUILD_SYSROOT a 'used variable'
        # so that conda-build knows to pass it to the test env
        - echo "{{ CONDA_BUILD_SYSROOT }}"  # [osx]
    requirements:
      run:
        - {{ pin_subpackage('openmpi', exact=True) }}
        # host C compilers work fine (better) on mac
        - {{ compiler('c') }}
    test:
      script: run_test.sh
      files:
        - tests/helloworld.c

  - name: openmpi-mpicxx
    build:
      script:
        - echo 'ok'  # [not osx]
        # make CONDA_BUILD_SYSROOT a 'used variable'
        # so that conda-build knows to pass it to the test env
        - echo "{{ CONDA_BUILD_SYSROOT }}"  # [osx]
    requirements:
      run:
        - {{ pin_subpackage('openmpi', exact=True) }}
        # host C compilers work fine (better) on mac
        - {{ compiler('cxx') }}
    test:
      script: run_test.sh
      files:
        - tests/helloworld.cxx

  - name: openmpi-mpifort
    build:
      script:
        - echo 'ok'  # [not osx]
        # make CONDA_BUILD_SYSROOT a 'used variable'
        # so that conda-build knows to pass it to the test env
        - echo "{{ CONDA_BUILD_SYSROOT }}"  # [osx]
    requirements:
      run:
        - {{ pin_subpackage('openmpi', exact=True) }}
        - {{ compiler('fortran') }}
    test:
      script: run_test.sh
      files:
        - tests/helloworld.f
        - tests/helloworld.f90
  {% else %}
  - name: openmpi
    build:
      string: {{ mpi_type }}_{{ build }}
      track_features:
        - openmpi_{{ mpi_type }}
    requirements:
      host: []
      run:
        - mpi 1.0 openmpi
    test:
      commands:
        - echo "It works!"
  {% endif %}

about:
  home: https://www.open-mpi.org/
  license: BSD-3-Clause
  license_family: BSD
  license_file: LICENSE
  summary: An open source Message Passing Interface implementation.
  description: |
    The Open MPI Project is an open source Message Passing Interface
    implementation that is developed and maintained by a consortium of academic,
    research, and industry partners.
  doc_url: https://www.open-mpi.org/doc/
  dev_url: https://github.com/open-mpi/ompi

extra:
  recipe-maintainers:
    - astrofrog
    - bekozi
    - dalcinl
    - leofang
    - minrk
    - msarahan
    - ocefpaf
    - beckermr
